name: allplaces_server
description: AllPlaces Serverpod server.

environment:
  sdk: '^3.8.0'

dependencies:
  serverpod: 3.4.4

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.4.4
  test: ^1.25.5

serverpod:
  scripts:
    start: dart bin/main.dart --apply-migrations
